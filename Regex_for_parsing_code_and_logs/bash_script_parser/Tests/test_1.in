#!/bin/bash

if [ -f /etc/bashrc ]; then
	 . /etc/bashrc
fi

alias a='b'
alias b='c'
alias c='d'
alias d='e'
alias e='f'
alias g='h'
alias i='-l'

dummy_function ()
{
	if [ -f file ]; then
		echo “1”
	else
		echo “2”
	fi
}
