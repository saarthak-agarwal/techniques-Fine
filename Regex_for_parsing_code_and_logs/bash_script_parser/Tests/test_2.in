#!/bin/bash

## Just a test. The code ~(makes any sense). ##
a=10 #Check if its removed after last function call
b=20
alias a='b'  #COMMENTNR
alias b='c' #COMMENTNR
alias c='a' #COMMENTNR

alias aa='bb'  #COMMENTNR
alias bb='cc' #COMMENTNR
alias cc='ad' #COMMENTNR

if [condition];then echo 'something'; fi

if [ $a == $b ] #EXAMPLE
then #Here also
   echo "a is equal to b"
fi #Here also

if [ $a != $b ]
then
   echo "a is not equal to b"
fi

echo -n "Enter a number: "
read VAR

if [[ $VAR -gt 10 ]]
then
  echo "The variable is greater than 10."
fi

echo -n "Enter a number: "
read VAR

if [[ $VAR -gt 10 ]]
then
  echo "The variable is greater than 10."
else
  echo "The variable is equal or less than 10."
fi

echo -n "Enter a number: "
read VAR

if [[ $VAR -gt 10 ]]
then
  echo "The variable is greater than 10."
elif [[ $VAR -eq 10 ]]
then
  echo "The variable is equal to 10."
else
  echo "The variable is less than 10."
fi

echo -n "Enter the first number: "
read VAR1
echo -n "Enter the second number: "
read VAR2
echo -n "Enter the third number: "
read VAR3

if [[ $VAR1 -ge $VAR2 ]]
then
  if [[ $VAR1 -ge $VAR3 ]]
  then
    echo "$VAR1 is the largest number."
  else
    echo "$VAR3 is the largest number."
  fi
else
  if [[ $VAR2 -ge $VAR3 ]]
  then
    echo "$VAR2 is the largest number."
  else
    echo "$VAR3 is the largest number."
  fi
fi

echo -n "Enter the first number: "
read VAR1
echo -n "Enter the second number: "
read VAR2
echo -n "Enter the third number: "
read VAR3

if [[ $VAR1 -ge $VAR2 ]] && [[ $VAR1 -ge $VAR3 ]]
then
  echo "$VAR1 is the largest number."
elif [[ $VAR2 -ge $VAR1 ]] && [[ $VAR2 -ge $VAR3 ]]
then
  echo "$VAR2 is the largest number."
else
  echo "$VAR3 is the largest number."
fi

# This is a Bash comment.
echo "This is Code" # This is an inline Bash comment.


# if [[ $VAR -gt 10 ]]; then
#  echo "Variable is greater than 10."
# fi

echo "Say Something"
<<COMMENT1
    your comment 1
    comment 2
    blah
COMMENT1
echo "Do something else"

read -p "Enter a password" pass
if test "$pass" = "Hamilton"
then
    # Again a comment
     echo "Password verified."
    #  This is a comment
else
     echo "Access denied."  
fi


dummy() #this function empty
{



} #end of humpty function

dummy1()
{



} #end of dumpty function

dummy2() #this function empty
{



}

dummy4()
{



}

dummy5() {} #end of sumpty function

function dummy #this function empty
{



} #end of humpty function

function dummy1
{



} #end of dumpty function

function dummy2 #this function empty
{



}

function dummy4
{



}

function dummy5 {} #end of sumpty function

#!/bin/bash
if [condition];then echo 'something'; fi

if [[ $VAR1 -ge $VAR2 ]]
then
  if [[ $VAR1 -ge $VAR3 ]]
  then
    echo "$VAR1 is the largest number."
  else
    echo "$VAR3 is the largest number."
  fi
else
  if [[ $VAR2 -ge $VAR3 ]]
  then
    echo "$VAR2 is the largest number."
  else
    echo "$VAR3 is the largest number."
  fi
fi

if [[ $VAR1 -ge $VAR2 ]]; then  if [[ $VAR1 -ge $VAR3 ]];  then    echo "$VAR1 is the largest number.";  else    echo "$VAR3 is the largest number."
  fi; else  if [[ $VAR2 -ge $VAR3 ]];  then    echo "$VAR2 is the largest number.";  else    echo "$VAR3 is the largest number.";  fi fi

if [condition];then echo 'something'; fi

if [ $a == $b ] #EXAMPLE
then #Here also
   echo "a is equal to b"
fi #Here also

if [ $a != $b ]
then
   echo "a is not equal to b"
fi

echo -n "Enter a number: "
read VAR

if [[ $VAR -gt 10 ]]
then
  echo "The variable is greater than 10."
fi

echo -n "Enter a number: "
read VAR

if [[ $VAR -gt 10 ]]
then
  echo "The variable is greater than 10."
else
  echo "The variable is equal or less than 10."
fi

echo -n "Enter a number: "
read VAR

if [[ $VAR -gt 10 ]]
then
  echo "The variable is greater than 10."
elif [[ $VAR -eq 10 ]]
then
  echo "The variable is equal to 10."
else
  echo "The variable is less than 10."
fi

echo -n "Enter the first number: "
read VAR1
echo -n "Enter the second number: "
read VAR2
echo -n "Enter the third number: "
read VAR3

if [[ $VAR1 -ge $VAR2 ]]
then
  if [[ $VAR1 -ge $VAR3 ]]
  then
    echo "$VAR1 is the largest number."
  else
    echo "$VAR3 is the largest number."
  fi
else
  if [[ $VAR2 -ge $VAR3 ]]
  then
    echo "$VAR2 is the largest number."
  else
    echo "$VAR3 is the largest number."
  fi
fi

echo -n "Enter the first number: "
read VAR1
echo -n "Enter the second number: "
read VAR2
echo -n "Enter the third number: "
read VAR3

if [[ $VAR1 -ge $VAR2 ]] && [[ $VAR1 -ge $VAR3 ]]
then
  echo "$VAR1 is the largest number."
elif [[ $VAR2 -ge $VAR1 ]] && [[ $VAR2 -ge $VAR3 ]]
then
  echo "$VAR2 is the largest number."
else
  echo "$VAR3 is the largest number."
fi

# This is a Bash comment.
echo "This is Code" # This is an inline Bash comment.


# if [[ $VAR -gt 10 ]]; then
#  echo "Variable is greater than 10."
# fi

echo "Says Something here"
<<COMMENT1
    your comment 1
    comment 2
    blah
COMMENT1
echo "Do something else"

read -p "Enter a password" pass
if test "$pass" = "jerry"; then    # Again a comment
     echo "Password verified."
    #  This is a comment
else     echo "Access denied.";fi

## #